'use client'
import { useEffect, useState } from 'react'
export function PortalMagicLinkActivation() { const [message,setMessage]=useState('Signing you in…'); useEffect(()=>{ const token=new URLSearchParams(window.location.search).get('token'); if(!token){setMessage('This sign-in link is invalid.');return} fetch('/api/auth/portal-magic-link/consume',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token})}).then(async r=>{if(!r.ok) throw new Error((await r.json()).error);window.location.assign('/portal')}).catch(e=>setMessage(e instanceof Error?e.message:'Unable to sign in.'))},[]); return <main className="grid min-h-screen place-items-center bg-[#050505] p-6 text-center text-[#f7efe3]"><p className="max-w-sm text-sm leading-6">{message}</p></main> }
